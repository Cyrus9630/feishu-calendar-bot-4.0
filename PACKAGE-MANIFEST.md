# 分享包内容说明

## 包含内容

- 飞书群日程机器人服务端源码；
- 日程解析、创建、查询、修改、取消、提醒和交互卡片能力；
- 妙搭云端运行看板和健康检查；
- 妙搭全栈项目配置、依赖锁文件和自动化测试；
- `先打开我.html` 和给 Codex 的一句话入口；
- Windows、macOS 双击部署入口及无第三方依赖的 Node.js 部署助手；
- `.env.example`、中文部署说明和包内自检脚本。

## 明确排除

- Windows 日程悬浮窗及其服务端配对、设备令牌和接口；
- 图片消息处理、图像识别及相应权限和依赖；
- 原项目的飞书 App ID、App Secret、令牌、群 ID、用户 ID、日历 ID；
- `.env`、`.env.local`、二维码、截图、运行日志、备份和数据库导出；
- Git 仓库、远程地址、提交历史、依赖缓存和构建产物；
- 原项目的开发计划、内部说明和本机绝对路径。

## 验收边界

分享包已经在全新解压目录完成依赖一致性、类型检查、自动化测试、生产构建和脱敏扫描。接收者仍需在自己的飞书开放平台与妙搭账号内创建应用、开通权限、配置事件和环境变量，并完成一次真实群消息验收。

## 安全提示

真实密钥只能保存到接收者自己的妙搭环境变量或本机安全秘密文件，不要写进源码、普通文档、截图、聊天记录或 Git。妙搭发布成功后，也不要把管理地址当作公开分享地址；运行范围由应用所有者按实际需要设置。

部署助手只在当前进程内存中短暂持有密钥，并通过标准输入把变量值交给妙搭 CLI。它不会把密钥放进命令参数、浏览器存储、部署报告或 Git，也不会覆盖接收者已经存在的项目目录。
